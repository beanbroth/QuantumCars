asset GameConfig;
asset AIConfig;

enum GameState
{
GameRunning,
GameSetup,
GameEnd,
GameReset
}

singleton component GameSessionManager 
{
    GameState CurrentGameState;
    asset_ref<GameConfig> fallbackGameConfig;
}

component SpawnPoint
{
    bool hasSpawnedFlag;
}

component AIController
{
    asset_ref<AIConfig> aiConfig;
}
